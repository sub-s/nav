import React from 'react';
import {View, Text, Button} from 'react-native';

const HashTag = ({navigation}) => {
  return (
    <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
      <Text>Hash Tag Screen</Text>
    </View>
  )
};

export default HashTag;
